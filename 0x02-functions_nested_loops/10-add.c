#include"main.h"
/**
 * add -  adds two integers and returns the result
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
int n = add(a, b);
n = 0;
while (n >= 0)
{
_putchar('n');
}
_putchar('\n');
return (n);
}
